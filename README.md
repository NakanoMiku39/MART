# Pull the image
Run `docker pull nakanomiku/mart:v1.0`

# Usage
1. Create an empty directory 
2. Put MART.zip and this Dockerfile in the directory
3. Run `docker build -t <image_name>:<version> .` under the directory


